spring:
  application:
    name: journey
management:
  endpoint:
    health.probes.enabled: true
    health.show-details: always
  endpoints.web:
    base-path: "/internal"
    exposure.include: health, prometheus
  health:
    livenessstate.enabled: true
    readinessState.enabled: true

sykmelding:
  topic: "tsm.tsm-sykmeldinger"
